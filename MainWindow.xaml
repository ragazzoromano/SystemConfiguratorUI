<Window
    x:Class="SystemConfiguratorUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:SystemConfiguratorUI.ViewModels"
    Title="{Binding WindowTitle}"
    Width="1100"
    Height="720"
    AllowDrop="True"
    DragOver="Window_DragOver"
    Drop="Window_Drop"
    mc:Ignorable="d">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>
    <Window.InputBindings>
        <KeyBinding Command="{Binding OpenCommand}" Gesture="Ctrl+O" />
        <KeyBinding Command="{Binding SaveCommand}" Gesture="Ctrl+S" />
        <KeyBinding Command="{Binding FindNextCommand}" Gesture="Ctrl+F" />
    </Window.InputBindings>
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Command="{Binding OpenCommand}" Header="_Open..." />
                <MenuItem Command="{Binding SaveCommand}" Header="_Save" />
                <MenuItem Command="{Binding SaveAsCommand}" Header="Save _As..." />
                <Separator />
                <MenuItem Command="{Binding ExitCommand}" Header="E_xit" />
            </MenuItem>
        </Menu>

        <ToolBarTray DockPanel.Dock="Top" IsLocked="True" Margin="5">
            <ToolBar>
                <Button Command="{Binding OpenCommand}" Content="Open" />
                <Button Command="{Binding SaveCommand}" Content="Save" />
                <Button Command="{Binding SaveAsCommand}" Content="Save As" />
                <Separator />
                <Button Command="{Binding ExpandAllCommand}" Content="Expand All" />
                <Button Margin="2,0,0,0" Command="{Binding CollapseAllCommand}" Content="Collapse All" />
                <Button Margin="8,0,0,0" Command="{Binding ExpandChildrenCommand}" Content="Expand Children" />
                <Button Margin="2,0,0,0" Command="{Binding CollapseChildrenCommand}" Content="Collapse Children" />
                <Separator />
                <TextBlock
                    Margin="8,0,4,0"
                    VerticalAlignment="Center"
                    Text="Search:" />
                <TextBox Width="180" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
                <Button
                    Margin="4,0,0,0"
                    Command="{Binding FindPreviousCommand}"
                    Content="Find Prev" />
                <Button
                    Margin="2,0,0,0"
                    Command="{Binding FindNextCommand}"
                    Content="Find Next" />
                <TextBlock
                    Margin="6,0,0,0"
                    VerticalAlignment="Center"
                    Text="{Binding SearchStatus}" />
                <Separator />
                <TextBlock
                    Margin="6,0,4,0"
                    VerticalAlignment="Center"
                    Text="Replace:" />
                <TextBox Width="140" Text="{Binding ReplaceText, UpdateSourceTrigger=PropertyChanged}" />
                <CheckBox
                    Margin="4,0"
                    Content="Match case"
                    IsChecked="{Binding MatchCase}" />
                <Button Command="{Binding ReplaceCommand}" Content="Replace" />
                <Button Command="{Binding ReplaceAllCommand}" Content="Replace All" />
            </ToolBar>
        </ToolBarTray>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <Border
                Padding="4"
                BorderBrush="LightGray"
                BorderThickness="0,0,1,0">
                <TreeView Margin="5"
                    x:Name="JsonTree"
                    ItemsSource="{Binding RootNodes}"
                    SelectedItemChanged="TreeView_SelectedItemChanged">
                    <TreeView.ItemContainerStyle>
                        <Style TargetType="TreeViewItem">
                            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                            <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                        </Style>
                    </TreeView.ItemContainerStyle>
                    <TreeView.Resources>
                        <HierarchicalDataTemplate DataType="{x:Type vm:JsonTreeNodeViewModel}" ItemsSource="{Binding Children}">
                            <StackPanel Orientation="Horizontal">
                                <StackPanel.ToolTip>
                                    <ToolTip>
                                        <StackPanel>
                                            <TextBlock FontWeight="Bold" Text="{Binding Name}" />
                                            <TextBlock Text="{Binding NodeType}" Foreground="Gray" />
                                            <TextBlock Text="{Binding DisplayValue}" MaxWidth="400" TextWrapping="Wrap" />
                                        </StackPanel>
                                    </ToolTip>
                                </StackPanel.ToolTip>
                                
                                <TextBlock Text="{Binding NodeTypeIcon}" 
                                           FontFamily="Segoe MDL2 Assets" 
                                           Margin="0,0,4,0" 
                                           Foreground="#FF666666" />
                                
                                <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" />
                                <TextBlock Text=": " />
                                <TextBlock Foreground="Gray" Text="{Binding DisplayValue}" />
                            </StackPanel>
                        </HierarchicalDataTemplate>
                    </TreeView.Resources>
                </TreeView>
            </Border>

            <GridSplitter
                Grid.Column="1"
                Background="Transparent"
                HorizontalAlignment="Stretch"
                Margin="2,0"
                ResizeBehavior="PreviousAndNext"
                ResizeDirection="Columns"
                ShowsPreview="True"
                Width="6" />

            <TabControl Grid.Column="2" Margin="5">
                <TabItem Header="Property Editor">
                    <Grid Margin="8" RowDefinitions="Auto,Auto,Auto,Auto,Auto,*">
                        <Grid.Resources>
                            <Style TargetType="TextBox">
                                <Setter Property="VerticalAlignment" Value="Center" />
                            </Style>
                        </Grid.Resources>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Margin="5"
                            FontWeight="SemiBold"
                            Text="Name:" />
                        <TextBox
                            Grid.Column="1"                            
                            Text="{Binding SelectedNode.Name, Mode=OneWay}" />

                        <TextBlock
                            Grid.Row="1"
                            Margin="5"
                            FontWeight="SemiBold"
                            Text="Type:" />
                        <TextBox
                            Grid.Row="1"
                            Grid.Column="1"
                            Text="{Binding SelectedNode.NodeType, Mode=OneWay}" />

                        <TextBlock
                            Grid.Row="2"
                            Margin="5"
                            FontWeight="SemiBold"
                            Text="Row:" />
                        <TextBox
                            Grid.Row="2"
                            Grid.Column="1"
                            IsReadOnly="True"
                            Text="{Binding SelectedNode.RowNumber, Mode=OneWay, TargetNullValue=''}" />

                        <TextBlock
                            Grid.Row="3"
                            Margin="5"
                            FontWeight="SemiBold"
                            Text="Editable:" />
                        <TextBlock
                            Grid.Row="3"
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            Text="{Binding SelectedNode.IsPrimitive, Mode=OneWay}" />

                        <TextBlock
                            Grid.Row="4"
                            Margin="5"
                            FontWeight="SemiBold"
                            Text="Value:" />
                        <StackPanel
                            Grid.Row="4"
                            Grid.Column="1"
                            Orientation="Vertical">
                            <TextBox
                                AcceptsReturn="True"
                                AcceptsTab="True"
                                FontFamily="Consolas"
                                HorizontalScrollBarVisibility="Auto"
                                MinHeight="120"
                                Text="{Binding SelectedNode.EditableValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                TextWrapping="NoWrap"
                                VerticalScrollBarVisibility="Auto">
                                <TextBox.Style>
                                    <Style TargetType="TextBox">
                                        <Setter Property="Visibility" Value="Visible" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SelectedNode.IsBoolean, Mode=OneWay}" Value="True">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBox.Style>
                            </TextBox>
                            <CheckBox Content="True/False" IsChecked="{Binding SelectedNode.BooleanValue, Mode=TwoWay}">
                                <CheckBox.Style>
                                    <Style TargetType="CheckBox">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SelectedNode.IsBoolean, Mode=OneWay}" Value="True">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </CheckBox.Style>
                            </CheckBox>
                        </StackPanel>
                    </Grid>
                    
                </TabItem>
                <TabItem Header="Raw JSON">
                    <DockPanel>
                        <StackPanel
                            Margin="8"
                            HorizontalAlignment="Right"
                            DockPanel.Dock="Top"
                            Orientation="Horizontal">
                            <Button
                                Margin="0,0,4,0"
                                Command="{Binding FormatJsonCommand}"
                                Content="Format" />
                            <Button Command="{Binding ValidateJsonCommand}" Content="Validate" />
                        </StackPanel>
                        <Grid Margin="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border
                                BorderBrush="LightGray"
                                BorderThickness="1,1,0,1"
                                Margin="0,0,2,0"
                                Background="#FFF7F7F7">
                                <ScrollViewer
                                    x:Name="LineNumbersScrollViewer"
                                    HorizontalScrollBarVisibility="Disabled"
                                    VerticalScrollBarVisibility="Hidden">
                                    <TextBlock
                                        x:Name="LineNumbersTextBlock"
                                        FontFamily="Consolas"
                                        FontSize="13"
                                        Padding="6,4"
                                        TextAlignment="Right" />
                                </ScrollViewer>
                            </Border>
                            <TextBox
                                x:Name="RawJsonEditor"
                                Grid.Column="1"
                                AcceptsReturn="True"
                                AcceptsTab="True"
                                FontFamily="Consolas"
                                FontSize="13"
                                HorizontalScrollBarVisibility="Auto"
                                Text="{Binding RawJsonText, UpdateSourceTrigger=PropertyChanged}"
                                TextChanged="RawJsonEditor_TextChanged"
                                TextWrapping="NoWrap"
                                VerticalScrollBarVisibility="Visible"
                                Loaded="RawJsonEditor_Loaded" />
                        </Grid>
                    </DockPanel>
                </TabItem>
            </TabControl>
        </Grid>
    </DockPanel>
</Window>
